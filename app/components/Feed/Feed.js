import React, { PropTypes } from 'react'

Feed.propTypes = {
  // : PropTypes.string.isRequired,
}

export default function Feed (props) {
  console.log(props)
  return (
    <div>{'Feed'}</div>
  )
}
